apiVersion: v1
kind: Service
metadata:
  name: {{ include "krato.fullname" . }}
  labels:
    {{- include "krato.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "krato.selectorLabels" . | nindent 4 }}
